<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Thewitcher" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema para la app, debe extender de MaterialComponents pues si no el
    ExtendedFloatingActionButton provoca errores -->
    <style name="Theme.Thewitcher" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorBoton</item>
        <item name="colorSecondary">@color/colorBoton</item>
        <item name="android:colorBackground">@color/colorFondo</item>
        <item name="android:statusBarColor">@color/colorFondo</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:editTextColor">@color/colorLetrasEditText</item>
    </style>

    <!-- Tema para la pantalla de carga -->
    <style name="Theme.CargaActivity" parent="Theme.Thewitcher">
        <item name="android:windowBackground">@drawable/imagen_carga</item>
    </style>
</resources>