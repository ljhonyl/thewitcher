<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Thewitcher" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MenuActivity" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorFondo</item>
        <item name="colorPrimaryVariant">@color/colorTarjetas</item>
        <item name="colorOnPrimary">@color/colorTarjetas</item>

        <!-- Color de la barra de notificaciones-->
        <item name="android:statusBarColor">@color/colorFondo</item>

    </style>
    <style name="Theme.Thewitcher" parent="Base.Theme.Thewitcher"/>
</resources>